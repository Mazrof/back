module.exports = {
  semi: true, // Enforce semicolons at the end of statements
  singleQuote: true, // Use single quotes for strings
  tabWidth: 2, // Indent code with two spaces
  useTabs: false, // Don't use tabs for indentation
  trailingComma: 'es5', // Include trailing commas (ES5 style)
  printWidth: 80, // Wrap lines at 80 characters
  bracketSpacing: true, // Use spaces inside brackets
  arrowParens: 'always', // Always enclose arrow function parameters in parentheses
  endOfLine: 'lf', // Use LF for line endings (Unix-style)
};
